# ── ROOT VARIANT SWITCH ────────────────────────────────────────────
# Change the target line below, push, and digitoxerai.app serves that
# variant. 200 = rewrite, so the URL bar still shows the bare domain.
# Only ONE of these may be uncommented at a time.

# /    /p/ally/index.html       200
/  /p/aspirant/index.html   200
# /  /p/guardian/index.html   200

# Each variant also stays reachable at its own address, always:
#   digitoxerai.app/p/ally/
#   digitoxerai.app/p/aspirant/
#   digitoxerai.app/p/guardian/
